build
dist
media
node_modules
package-lock.json
.env
.next
.vercel

# Local credentials; never commit service-account keys or private key material.
gcs-credentials.json
service-account*.json
*-credentials.json
*.pem
*.key

# Local legacy snapshots; exports may contain private content and stay uncommitted.
/.legacy-export/

# R1 build and test artifacts.
.pnpm-store
coverage
*.tsbuildinfo
